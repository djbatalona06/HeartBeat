export function WorkPage() {
  return (
    <div className="page">
      <header className="page-head">
        <h1 className="page-title">Work</h1>
        <p className="page-sub">A calendar you both can see.</p>
      </header>
      <div className="empty">
        <p>Not built yet.</p>
        <p className="empty-sub">
          The data model and the sync path are in place; this screen is the next
          piece of work. See <code>docs/DESIGN.md</code>.
        </p>
      </div>
    </div>
  );
}
